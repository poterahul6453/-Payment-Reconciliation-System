DATABASE_URL=sqlite+aiosqlite:///./reconciliation.db
REPORTS_DIR=../reports
UPLOADS_DIR=../uploads
# Optional; auto-detects backend/sample_data or repo sample_data/ when unset
# SAMPLE_DATA_DIR=../sample_data
TOLERANCE=0.01
LOG_LEVEL=INFO
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,https://payment-reconciliation-system.vercel.app

# PostgreSQL (Docker)
# DATABASE_URL=postgresql+asyncpg://recon:recon@localhost:5432/reconciliation
